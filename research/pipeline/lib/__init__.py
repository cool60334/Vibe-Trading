"""
research/pipeline/lib/
──────────────────────
Pure-function helpers shared across pipeline stages.

Modules:
  - factor_selector: deterministic candidate selection from evidence
    (used by stage0_discovery to replace swarm-primary mode).
"""
